mutation InsertMessage($message: String!, $threadId: uuid!, $selfId: uuid!) {
  insert_messages(objects: {
    message: $message,
    thread_id: $threadId,
    user_sent: $selfId
  }) {
    returning {
      created_at
    }
  }
}