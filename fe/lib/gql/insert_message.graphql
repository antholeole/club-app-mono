mutation InsertMessage($message: String!, $threadId: uuid!, $selfId: uuid!, $messageId: uuid) {
  insert_messages(objects: {
    message: $message,
    thread_id: $threadId,
    user_sent: $selfId,
    id: $messageId
  }) {
    returning {
      created_at
    }
  }
}